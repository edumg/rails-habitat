 <% content_for(:title_tag) do %>
  Habitat - <%= current_user.profile.first_name %> <%= current_user.profile.last_name %>
 <% end %>


<% if current_user.profile.is_host %>
  <%= render partial: 'host_profile' %>
<% else %>
  <%= render partial: 'renter_profile' %>
<% end %>

